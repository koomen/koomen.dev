/*** src/posts/first.mdx ***/
import { Button } from '../components/Button'

export const meta = {
  title: 'Why Vite + MDX rocks',
  date: '2025-04-27',
  tags: ['vite', 'mdx', 'react']
}

# {meta.title}

Welcome! MDX lets you drop components straight into Markdown:

<Button onClick={() => alert('Hi there!')}>Say Hi</Button>